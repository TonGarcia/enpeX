<%= form_for(@description_item) do |f| %>
  <% if @description_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@description_item.errors.count, "error") %> prohibited this description_item from being saved:</h2>

      <ul>
      <% @description_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :long_description %><br>
    <%= f.text_field :long_description %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
